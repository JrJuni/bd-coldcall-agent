# Phase 1+ — core (search, config, tests)
httpx>=0.27.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
pyyaml>=6.0.2

# Phase 4+ — Claude API
anthropic>=0.40.0

# Phase 5+ — LangGraph orchestration
langgraph>=0.2.0
langchain-core>=0.3.0

# Phase 1.5+ — article body extraction
trafilatura>=1.12.0

# Phase 3+ — RAG connectors (non-ML)
notion-client>=2.2.0
pypdf>=4.0.0

# Phase 6+ — CLI
typer>=0.12.0

# Phase 7+ — Web API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0
langgraph-checkpoint-sqlite>=2.0.0
# Phase 13C M8 — Postgres-backed LangGraph checkpointer. Installed
# unconditionally; the saver itself is only constructed when DATABASE_URL
# is a postgresql:// URL, so SQLite-only boxes pay nothing at runtime.
langgraph-checkpoint-postgres>=2.0.0
# Phase 10 P10-3 — multipart/form-data uploads (UploadFile)
python-multipart>=0.0.9

# Phase 13A — ORM seam (dual-engine SQLite ↔ Postgres)
sqlalchemy>=2.0.0
alembic>=1.13.0
# psycopg3 — only the binary distribution is needed for Postgres support;
# SQLite-only dev boxes still work fine because the dialect is registered
# lazily when DATABASE_URL starts with postgresql://.
psycopg[binary]>=3.2.0

# Phase 13A — MCP server (stdio for Claude Desktop)
mcp>=1.0.0

# Test
pytest>=8.0.0

# Heavy ML / vector deps are split out — see requirements-ml.txt
# (install separately with correct CUDA/CPU torch index URL)
