import StubPage from "@/components/StubPage";

export default function DiscoverPage() {
  return (
    <StubPage
      title="Discovery"
      ship="P10-2"
      description="candidates.yaml 을 임포트해 sortable·editable 테이블로 후보를 검수합니다. 6 차원 점수 / weights 슬라이더로 LLM 호출 0원 tier 재계산. 마음에 드는 후보는 Targets 탭으로 promote."
    />
  );
}
