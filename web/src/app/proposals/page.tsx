import StubPage from "@/components/StubPage";

export default function ProposalsPage() {
  return (
    <StubPage
      title="Proposals"
      ship="P10-4"
      description="제안서 목록 + 신규 작성 + 결과 편집/재생성/다운로드. 현재는 홈 화면의 'New Run' 폼이 그대로 동작합니다."
    />
  );
}
