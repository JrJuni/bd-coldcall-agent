import StubPage from "@/components/StubPage";

export default function TargetsPage() {
  return (
    <StubPage
      title="Targets (파이프라인)"
      ship="P10-1"
      description="등록된 타겟 기업과 영업 단계 (planned / contacted / proposal_sent / meeting / won / lost) 관리. Discovery 에서 promote 한 기업과 수동 추가 기업이 모이고, 행 클릭으로 Proposal 생성·콜 기록 jump."
    />
  );
}
